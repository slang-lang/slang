#!/bin/bash

COMMAND="/path/to/ObjectiveScript/trunk/release/bin/oscript/oscript"
LIBRARY="/path/to/ObjectiveScript/Library"
USER=`whoami`

if [ ${USER} == "www-data" ]; then
	echo -e "Content: text/html\n"
fi

${COMMAND} --library ${LIBRARY} $*

